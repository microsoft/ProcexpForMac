//
//  ContentView.swift
//  Hosts the W3 main window (process tree table, toolbar, status bar).
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        MainWindowView()
    }
}
